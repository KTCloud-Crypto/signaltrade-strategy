[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "signaltrade-strategy"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "boto3==1.40.21",
  "fastapi==0.116.1",
  "httpx==0.28.1",
  "prometheus-client==0.22.1",
  "psycopg2-binary==2.9.10",
  "pydantic-settings==2.10.1",
  "pyupbit==0.2.31",
  "SQLAlchemy==2.0.43",
  "uvicorn[standard]==0.35.0",
  "websockets==15.0.1",
]

[project.scripts]
signaltrade-strategy-worker = "signaltrade_strategy.worker:run"

[project.optional-dependencies]
dev = ["pytest==8.4.2"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
